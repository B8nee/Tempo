import Phaser from 'phaser';

class GameRick extends Phaser.Scene {

    constructor() {
        super({ key: "GameRick"});
    }

    preload() {
    }

    create() {
       
    }
}

export default GameRick;